# Project-local default for the independent ESA research sessions.
# Authentication stays in the existing user-level ChatGPT login.
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
# Full Access requested by the architect; no filesystem sandbox or approvals.
# Research authority boundaries in AGENTS.md still apply.
sandbox_mode = "danger-full-access"
approval_policy = "never"
